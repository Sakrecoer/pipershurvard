# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: "Pipers Hudvård"
email: pipershudvard@gmail.com
phone: +46 70 123 45 67
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://pipershudvard.com" # the base hostname & protocol for your site, e.g. http://example.com
instagram_username: pipershudvard
# Values for the jekyll-seo-tag gem (https://github.com/jekyll/jekyll-seo-tag)
logo: /siteicon.png
description: >- # this means to ignore newlines until "baseurl:"
  Write an awesome description for your new site here. You can edit this
  line in _config.yml. It will appear in your document head meta (for
  Google search results) and in your feed.xml site description.
author:
  name: 
  email:
  twitter: # twitter username without the @ symbol
social:
  name: En Renare Vardag AB
  links:
    - https://enrenarevardag.se

# Build settings
plugins:
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll-feed
  - jekyll-paginate-v2

# Plugin settings:

# excerpt_separator: '<!--more-->'
pagination:
  enabled: true

# Site Variables:
boka: https://boka.itsperfect.se/31782333/Kund/Loggain?ReturnUrl=%2F31782333
clinicare: https://www.beauty-bar.se/produkt-kategori/varumarken/cliniccare/?ref=14

# collection
collections:
  # landningssida:
  #   output: true
  #   permalink: /:path/
  behandlingar: 
    output: true

# defaults
timezone: Europe/Stockholm
permalink: /:categories/:title/
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      permalink: /:year/:month/:day/:title/
  - scope:
      path: ""
      type: "behandlingar"
    values: 
      layout: "post"
sass:
    sass_dir: /assets/sass/